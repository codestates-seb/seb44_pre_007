export type Props = {
  children: string | JSX.Element | JSX.Element[];
  primaryColor: string;
  hoverColor: string;
};

export interface IconStyle {
  width: string;
  height: string;
  color: string;
}
