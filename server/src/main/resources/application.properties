aws.paramstore.enabled=true
aws.paramstore.prefix=/spring-boot-aws
aws.paramstore.profile-separator=_
aws.paramstore.name=chat7pt

spring.config.activate.on-profile=aws
spring.config.import=aws-parameterstore:
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}

jwt.key=${jwt.key}
spring.security.oauth2.client.registration.google.clientId=${spring.security.oauth2.client.registration.google.clientId}
spring.security.oauth2.client.registration.google.clientSecret=${spring.security.oauth2.client.registration.google.clientSecret}

#spring.security.oauth2.client.registration.google.clientId=${GOOGLE_CLIENTID}
#spring.security.oauth2.client.registration.google.clientSecret=${GOOGLE_SECRET}



